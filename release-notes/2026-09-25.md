# 2026-09-25

- **02_data_center — the DC VXLAN EVPN fabric is now automated, built on Cisco
  Nexus as Code.** `02_data_center` was a one-paragraph stub naming services
  that are actually driven from other collections. It is now a working
  pipeline, `nac_vxlan/ansible/`, covering the guide's **NDFC - DC Fabric
  Deployment** section end to end: the `Pseudoco-DC1` fabric, its five
  switches and their roles, the DC-Leaf1/DC-Leaf2 vPC pair, the three server
  port-channels, and the MAIN / PROD / IOT VRFs and networks with their
  attachments.

  Built on `cisco.nac_dc_vxlan` 0.9.0 over `cisco.dcnm` 3.13.0 rather than on
  `cisco.dcnm` alone. The reason is the shape of the problem: `cisco.dcnm`
  gives one module per Nexus Dashboard object, so automating this section with
  it directly means transcribing the student's click sequence, in order, with
  every dependency between steps written out by hand. Nexus as Code takes a
  declared end state and works out the calls, which is both shorter and the
  thing that survives a change to the fabric.

  `cisco.nd` was evaluated and dropped. All 26 of its modules are
  ND-platform-level — sites, backups, Insights, `nd_rest`, `nd_version` —
  with nothing for fabrics, VRFs, networks or interfaces, so it would have
  added a dependency and a Python 3.11 floor for no capability.

  The collection lives at the same depth as `01_campus/evpn/ansible`, so
  `ansible.cfg`'s `../../../../.vault` and `../../../plugins/vars` paths are
  byte-identical to the campus ones and credentials come from the same
  vault-encrypted `lab_access.yml`. Nothing in the new tree holds a password.

- **02_data_center — laid out like `01_campus/evpn`, with one forced
  exception.** `ansible.cfg` at the collection root, `inventory/` holding
  `static_inventory.yml` and `group_vars/`, playbooks under `playbooks/`, and
  `evidence/` written beside them using the campus collection's
  `(playbook_dir ~ '/../evidence/…') | realpath` idiom. Run from the directory
  holding `ansible.cfg`, same as campus, because `ansible.cfg`'s relative
  paths to the vault and the vars plugin resolve against the working
  directory.

  The exception is `host_vars/`, which sits under `playbooks/` rather than
  under `inventory/` where Ansible's own convention would put it.
  `roles/validate/tasks/sub_main.yml` in `cisco.nac_dc_vxlan` passes the model
  location as a task-level variable, `data_path: "{{ playbook_dir }}/host_vars/{{ inventory_hostname }}"`,
  and task-level vars are beaten only by `-e`, so there is no override and the
  data model has to be a sibling of the playbooks. A symlink was considered and
  rejected: the failure mode when it is missing is an empty model, and a
  load-bearing symlink is a worse thing to hide than an unusual directory.
  `group_vars/` is plain Ansible with no collection involvement, so it does
  live under `inventory/`. Both the collection README and the inventory file
  record why.

- **02_data_center — serial number discovery is its own stage, because serials
  cannot be committed.** Nexus as Code identifies a switch by `serial_number`,
  not by hostname or management IP. That is the right identifier for a fabric
  but wrong for a shared repository: serials belong to one dCloud pod, so
  committing them would tie the checkout to a single instance and leave every
  other student's tree wrong — the same class of problem that made `lab.yml`
  gitignored on 2026-09-16.

  `00_discover_dc_switch_serials.yml` SSHes to each management address in the
  declared switch table (`group_vars/all/dc_switches.yml`) and runs
  `cisco.nxos.nxos_facts`. It is read-only with respect to the switches —
  `nxos_facts` issues show commands and nothing else — so it never pushes
  configuration to a device.

  As first written the stage printed the serials and wrote nothing, on the
  argument that transcribing them into
  `host_vars/Pseudoco-DC1/topology_switches.nac.yaml` was a deliberate manual
  step where the student saw what Nexus as Code keys a switch on.
  **That was reversed later the same day: the stage now generates that file
  itself. See the last entry below, which is the current behaviour.** What has
  not changed is that the file is gitignored with a tracked `.example` beside
  it, so it never makes a student's checkout dirty, and that the stage is not
  imported by `01_dc_deploy.yml`; if it is skipped, the orchestrator stops at
  stage 03 and says why.

  Stage 03 guards both failure modes, because the collection will not. Rule
  301 only checks that `serial_number` is non-empty, so a leftover
  `REPLACE_ME` passes validation and then fails deep inside the create role
  where the message means nothing. Stage 03 therefore refuses to start if the
  model file is missing or if any placeholder is still in it.

  Worth recording, because it is the reason this stage has to exist at all:
  the import does not use the serial. `fabric_inventory.j2` imports each
  switch by `seed_ip` with `max_hops: 0`, and `serial_number` appears there
  only inside the POAP blocks. So the collection demands a value it does not
  need for the one operation that could produce it, and a serial cannot be
  read out of the fabric until the switch is already in the fabric.

  Two findings are baked into the design. The discovery endpoint is scoped to
  a fabric, so the fabric must exist before any switch can be discovered while
  the switches are what the rest of the model needs; `02_fabric.yml` exists
  purely to break that circle, and upstream shipping a `fabric_empty_example`
  confirms a switchless fabric is a supported state. And the serials are
  written as literal text rather than as a lookup, because the validate role
  reads the model off disk in Python and never renders Jinja in it — an
  expression in a `.nac.yaml` file reaches the validator verbatim.

  It uses `dcnm_rest`, not `dcnm_inventory`. `dcnm_inventory`'s
  `update_discover_params()` filters the response down to the single entry
  matching `seedIP`, discarding the other four switches — which would look
  like a successful discovery that found one switch.

- **02_data_center — the build is greenfield by default and there is no
  confirmation gate.** The guide builds this fabric with **Preserve Config**
  unchecked, so Nexus Dashboard erases each switch's running configuration as
  it imports it, and the pipeline does the same. An earlier revision tried to
  gate this behind `-e dc_greenfield_confirm=WIPE_OK`, keyed off the declared
  `greenfield_cleanup` value. That gate has been removed because it was
  guarding the wrong thing: `preserve_config: false` is hardcoded in
  `cisco.nac_dc_vxlan`'s inventory template, so the import was always going to
  be destructive whatever the data model said, and a flag that implies
  otherwise is worse than no flag. The warning now lives in the docs instead.

- **02_data_center — `greenfield_cleanup` corrected from `Disable` to
  `Enable`.** It renders `GRFIELD_DEBUG_FLAG`, which Cisco documents as
  "Greenfield Cleanup Option — Enable to clean switch configuration without
  reload when PreserveConfig=no". That is the Advanced-tab dropdown the guide
  labels **Add Switches without Reload** and tells you to enable, and Cisco
  recommends it for Nexus 9000v fabrics, which is what this pod runs. The
  previous `Disable` was based on reading it as an equivalent of Preserve
  Config, which it is not; the practical effect of the fix is that the five
  switches no longer reload one by one during import.

  `06_remove.yml` carries a separate confirmation (`dc_remove_confirm=REMOVE_OK`)
  and is deliberately absent from `01_dc_deploy.yml`. Even when confirmed it
  leaves `inventory_delete_mode` and `link_vpc_delete_mode` off, because
  evicting switches or breaking the vPC pair is not something a routine prune
  should do — and the data model documents vPC pairs as
  provision-and-decommission only, so a broken pair has to be recreated rather
  than edited back.

- **02_data_center — verification is Nexus Dashboard API only, so it works with
  the switch VPN down.** `05_verify_fabric.yml` reads the declared intent from
  the same `.nac.yaml` files the create role consumes, so the report cannot
  drift from what was deployed, and compares it against
  `switchesByFabric` plus the top-down VRF, network and attachment endpoints.
  No device SSH. It writes `evidence/stage05-verification.md` in the same shape
  as the campus stage 12 report and fails on mismatch unless
  `-e dc_verify_fail_on_mismatch=false`.

  The check that matters most is `lanAttachState`: an attachment sitting in
  `PENDING` means intent exists on the controller and never reached the switch,
  which is exactly the failure mode that a "deployment succeeded" message hides.
  Config compliance is reported when the controller supplies it but is not
  asserted on, because `ccStatus` does not appear anywhere in `cisco.dcnm`
  3.13.0 and asserting on an unverified key would produce false failures.

- **02_data_center — the upstream collection's own examples are stale, and the
  README says so.** `tests/integration/host_vars/examples/` inside
  `cisco.nac_dc_vxlan` 0.9.0 still uses the pre-1.0 flat shape:
  `vxlan.global.name`, `vxlan.global.bgp_asn`,
  `vxlan.underlay.general.subnet_mask`. The current model is
  `vxlan.fabric.name` / `vxlan.fabric.type`, `vxlan.global.ibgp.bgp_asn` and
  `vxlan.underlay.ipv4.subnet_mask` — confirmed against
  `roles/validate/files/defaults.yml`, `plugins/plugin_utils/data_model_keys.py`
  and the deprecation path in `prep_001_fabric.py`, which explicitly describes
  the flat keys as retained for backwards compatibility.

  This is worth more than a footnote because of how the collection fails.
  `schema_path` defaults to empty and the fallback schema does not exist, so
  schema validation is **skipped with a warning** and only the Python rules
  run. A key that is misspelled or in the wrong place is therefore dropped
  silently rather than rejected. Following the shipped examples would have
  produced a model that validated cleanly and built the wrong fabric.

- **02_data_center — the pipeline is now at the limit of what Nexus as Code
  0.9.0 can express, and the remainder is written down as a TODO rather than
  left implicit.** Guide sections 4, 5 and 6 are covered end to end. Sections
  7 and 8 are outside the data model. The coverage boundary was established by
  dumping every nvPair the fabric templates can emit — 134 for a VXLAN EVPN
  fabric, 30 for an External one — and checking the guide's settings against
  that list, rather than by grepping for names that might be wrong. The
  command to repeat the exercise on a future release is in the collection
  README.

  Four verified gaps, each with the evidence: no `vrf_lite` key on a VRF
  attachment (`dc_vxlan_fabric_vrfs.j2` emits only `- ip_address`); no
  `VRF_LITE_AUTOCONFIG` / `DCI_SUBNET_RANGE` / `AUTO_SYMMETRIC_VRF_LITE` /
  `AUTO_UNIQUE_VRF_LITE_IP_PREFIX`; no way to set Fabric Monitor Mode; and no
  device type with which to import the IOS-XE edge router.

  `overlay_extensions.vrf_lites` is **not** the missing feature despite the
  name. It renders `ndfc_vrf_lite_ebgp.j2`, which is `router bgp` / `neighbor`
  CLI pushed as a switch policy — no sub-interface, no attachment. Used alone
  it would configure a BGP neighbour with no interface to reach it. The README
  records what a real VRF-Lite stage needs, including that the create role
  runs `dcnm_vrf` with `state: replaced` and would detach DC-Service-Leaf on
  every run, so such a stage must run *after* create each time.

- **02_data_center — `07_external_fabric.yml` now refuses to touch an
  External fabric that already exists, because Nexus as Code does not fail
  safe here.** `dc_external_fabric_general.j2` does not omit the Monitor Mode
  nvPair, it emits `IS_READ_ONLY: false`, and the create role applies the
  fabric with `dcnm_fabric state: merged`. Against the pod's real External
  fabric — which is created in Monitor Mode with the IOS-XE edge router
  already discovered into it — running that stage would have handed Nexus
  Dashboard write access to a device it is meant only to observe. A pre_task
  now GETs the fabric and stops if it is there.

- **02_data_center — corrected: the edge router is a prerequisite, not out of
  scope.** `DC-SITE11-CEDGE8Kv` is added to the External fabric in Nexus
  Dashboard before any of this automation runs. Earlier notes in six files
  claimed the External fabric was "switchless by design" and that VRF-Lite
  could not be automated because Nexus Dashboard had "no far end to compute
  the inter-fabric connection against". Both were wrong. The far end exists;
  the blocker is only the missing data model key. Corrected in the External
  data model, `07_external_fabric.yml`, `static_inventory.yml`,
  `01_dc_deploy.yml`, the stage 05 report template, both READMEs and the
  student guide.

- **pins — `cisco.nac_dc_vxlan` 0.9.0 and `cisco.dcnm` 3.13.0 added to the
  bootstrap, with the Python dependencies they actually need.** Both pins are
  in `00_scriptserver_bootstrap/.../files/requirements.yml` as well as the new
  collection `requirements.yml`, identical in both, because the bootstrap file
  is the one that gets installed — a higher pin in a collection file alone is
  silently never applied, which is the trap `cisco.catalystcenter` fell into on
  2026-09-15.

  ND 4.2.1 support landed in `cisco.dcnm` 3.12.1 and `cisco.nac_dc_vxlan` 0.9.0
  requires `>= 3.13.0`, so 3.13.0 is a floor rather than a preference.
  `ansible-automation/requirements.txt` gains `nac-yaml==1.0.0`,
  `nac-validate==1.0.0`, `macaddress`, `packaging`, `jmespath` and `requests`:
  the collection reads and validates the data model in Python, so these are
  hard runtime dependencies, not optional extras. The two pinned versions are
  the ones the collection's own `requirements.txt` pins. All six confirmed to
  exist on PyPI and to be compatible with the repo's `ansible-core==2.17.14`
  (`requires_ansible: ">=2.15.0,<2.19.0"`).

  Follow-up the same day: the two files were not actually identical, despite
  both carrying a comment saying they must be. The DC file pinned
  `ansible.netcommon` 7.1.0, `ansible.utils` 5.1.2, `ansible.posix` 2.0.0 and
  `community.general` 10.1.0 — the versions in the upstream
  `netascode/ansible-dc-vxlan-example` — and the bootstrap pinned none of
  them, so on the script server they arrived as `cisco.dcnm` dependencies at
  whatever the Galaxy resolver picked. All four added to the bootstrap. The
  chosen values also satisfy the campus ranges (`ansible.posix >=1.5.0`,
  `community.general >=8.0.0,<11.0.0`), and all six were confirmed to declare
  `requires_ansible >=2.15.0`, so nothing is constrained below the repo's
  `ansible-core==2.17.14`.

- **script server — `ansible-pylibssh` 1.4.0 pinned, so `network_cli` stops
  falling back to a deprecated transport.** Stage 00 of the DC collection is
  the first thing in this repo to use `ansible.netcommon.network_cli`, and it
  prints `[WARNING]: ansible-pylibssh not installed, falling back to paramiko`
  on a script server built before this pin.

  The warning is benign — `network_cli`'s `ssh_type` defaults to `auto`, it
  imports `ansible-pylibssh` if present and otherwise uses paramiko, which is
  pinned and works. The reason to fix it anyway is in netcommon's own option
  documentation: the paramiko path is **deprecated for removal in a release
  after 2028-02-01**, with an explicit instruction to migrate to libssh. So
  silencing the warning by pinning `ssh_type: paramiko` would have locked the
  lab to the transport being removed. Installing the library satisfies it
  properly and is faster, since libssh runs in-process.

  This does not repeat the `sshpass` mistake. That failure was an **apt**
  package pulling `libc6` forward until apt refused against the pinned
  `libc6-dev`. `ansible-pylibssh` publishes manylinux wheels to PyPI with
  libssh bundled (`manylinux_2_27_x86_64`, cp39 through cp314), so pip
  downloads a prebuilt binary into `~/venv` with no apt package, no compiler
  and no glibc change.

  Existing script servers need `pip install -r ansible-automation/requirements.txt`
  inside `~/venv`, or a re-run of `01_bootstrap_script_server.yml`. Until then
  the warning is cosmetic and stage 00 still collects serials.

- **02_data_center — serial discovery renamed to
  `00_discover_dc_switch_serials.yml` and rewritten against `cisco.nxos`
  instead of the Nexus Dashboard API.** It now SSHes to each switch and runs
  `cisco.nxos.nxos_facts`; the default `gather_subset: min` resolves to the
  `default` legacy subset, whose `Default.populate()` runs `show version` and
  parses the Processor Board ID into `ansible_net_serialnum` (verified in
  `module_utils/network/nxos/facts/legacy/base.py`, not assumed).

  The rename is not cosmetic. The old stage used
  `POST .../fabrics/{fabric}/inventory/test-reachability`, which is
  **fabric-scoped**, so the fabric had to be created before any serial could
  be read — and that is the only reason `02_fabric.yml` existed and the only
  reason discovery used to sit at stage 02, under its old name. Reading the
  switches directly removes the ordering constraint, so discovery is
  genuinely step zero. `02_fabric.yml` is kept as an explicit cheap first
  step mapping to the guide's "Create a VXLAN Fabric", but it is no longer
  load-bearing: stage 03 would create the fabric on its own.

  Discovery is also **no longer imported by `01_dc_deploy.yml`.** It reaches
  a switch rather than the controller, and it only needs re-running when the
  pod hardware changes, so it is a prerequisite the student runs once — like
  the External fabric. Stage 03 still refuses to start until the serials are
  in the model, so forgetting is caught.

  The trade to be aware of: this is now the **only** stage in the collection
  that needs SSH to `198.18.128.x`. Everything else reaches only the Nexus
  Dashboard API and works with the switch path down.

  The switch list is not duplicated into `static_inventory.yml`. The
  `dc_fabric_switches` group is declared there empty and filled at run time
  with `add_host` from `dc_switches`, so names and addresses have one source
  of truth and cannot drift; its SSH settings are in the new
  `inventory/group_vars/dc_fabric_switches/connection.yml`. Verified that
  group_vars do bind to hosts created by `add_host` when the group is
  pre-declared — `ansible_network_os`, the vault-sourced credentials and the
  60s timeouts all resolved on all five hosts in a smoke run.

  `cisco.nxos` 10.2.0 was already pinned in the bootstrap requirements but
  missing from the collection's own `collections/requirements.yml`; added, so
  the two stay identical and a student who installs only from the collection
  gets it.

  Dropped as dead: `dc_discovery_seed_ip`, `dc_discovery_max_hops`,
  `dc_discovery_snmp_auth_protocol` and `dc_discovery_cdp_second_timeout`,
  which only the old crawl used. `ndfc_switch_username`/`ndfc_switch_password`
  in `group_vars/nd/` **stay** — the collection's own
  `plugins/action/common/get_credentials.py` reads those exact key names off
  the ND host's vars to substitute `PLACE_HOLDER_USERNAME`/`PLACE_HOLDER_PASSWORD`
  in `fabric_inventory.j2`, and fails the run if either is empty.

- **00_scriptserver_bootstrap — no playbook runs git any more. Refreshing the
  checkout is a plain `git pull`.** `playbooks/02_sync_from_git.yml` and
  `roles/script_server_bootstrap/tasks/sync_repo.yml` are both **deleted**.

  Prompted by a live failure: a student with local edits got `Local
  modifications exist in the destination: … (force=no)` after three retries,
  with `ok=29 changed=3` — packages, the venv, PATH and the Galaxy collections
  had all already succeeded, and the bootstrap failed anyway over uncommitted
  work that had nothing to do with bootstrapping.

  The pull inside the bootstrap was the root problem, and it was worse than a
  bad error message. The role runs from the checkout it was rewriting, and it
  reads `requirements.txt` in `venv.yml`, the Galaxy pins and its own
  `defaults/main.yml` off disk *before* the point where the pull sat — so the
  pull could not inform the work that followed it. All it achieved was making
  `defaults/main.yml` disagree with the task files for the rest of the play,
  a hazard `http_image_server.yml` still carries a `| default()` guard for.

  `02_sync_from_git.yml` then went too, because it only wrapped
  `ansible.builtin.git`: it did what `git pull` does while obscuring what was
  happening, and having it there is what made a pull look like a legitimate
  bootstrap step in the first place. Students run `git pull`, then re-run the
  bootstrap only if the release note says a pin moved.

  Kept: `tasks/pod_files.yml`, split out of the old file, which seeds
  `lab.yml` from the tracked example and checks `.vault`. Both are gitignored,
  so no git operation creates or protects them — which is exactly why the role
  has to.

  Removed with them: the `script_server_git_repo` and `script_server_git_version`
  defaults, now unread. The second was also a latent collision —
  `verify.yml` registered `script_server_git_version` for the output of
  `git --version`, silently shadowing the branch name. That register is now
  `script_server_git_binary_version`.

  Updated in the same pass: both bootstrap and campus READMEs, the root
  README, the DC collection README, the student guide's Lab Prep table and DC
  automation card.

- **02_data_center — installing the collections is now an explicit step in
  both READMEs and the student guide, instead of being assumed.** It was
  missing everywhere: the collection README went straight to "Running it", the
  track README's Quick start was two lines, and guide card 10 showed only
  `ansible-playbook`. A script server staged before the DC track existed has
  neither `cisco.nac_dc_vxlan` nor `cisco.dcnm`, so the very first command a
  student runs fails.

  The collection README gains a **Before you run anything** section covering
  the bootstrap re-run, the narrower `ansible-galaxy install -r ... --force`,
  the `ansible-galaxy collection list` check with the versions to expect, and
  the External fabric prerequisite. It also gains a **Troubleshooting** table,
  matching the campus README's symptom → cause → fix shape.

  Called out in all three places, because the error names the wrong thing:
  Ansible reports a missing *collection* as a missing *role* —
  `the role 'cisco.nac_dc_vxlan.validate' was not found` followed by role
  search paths — which sends you looking at the playbook rather than at what
  is installed. Hit live on the Kali script server, whose `~/venv` predates
  `4c6ca7a`. `--force` is flagged in each place for the reason it bit campus:
  without it `ansible-galaxy` silently skips a collection already present at
  any version.

  Also renamed the README's "Coverage, and what is left — TODO" heading to
  drop the em dash, which made the GitHub anchor unstable, and added an
  anchor check to the verification pass.

- **docs — the student guide gains an optional card 10, "Automating the DC
  Fabric".** Same treatment the assurance section gets: it does not replace the
  manual walkthrough, it explains what the automation does differently and why.
  It covers the declarative model with a worked example, why serial discovery
  has to be its own stage, that the import is greenfield and wipes the
  switches, and an honest account of why guide sections 7 and 8 stay manual.
  Nav entry added alongside; `<div>` balance verified unchanged (836 → 841,
  balanced before and after).

- **02_data_center — the DC playbooks are renumbered so that the number tells
  you whether the orchestrator runs the stage.** `00_dc_deploy.yml` becomes
  `01_dc_deploy.yml`, `01_fabric.yml` becomes `02_fabric.yml`,
  `06_verify_fabric.yml` becomes `05_verify_fabric.yml` and `05_remove.yml`
  becomes `06_remove.yml`. The verify stage's report template moves with it,
  `templates/stage06-verification.md.j2` → `templates/stage05-verification.md.j2`,
  so the evidence file a run leaves behind is now
  `evidence/stage05-verification.md`. Every rename went through `git mv` so
  the history follows the file, and all eight playbooks pass
  `ansible-playbook --syntax-check` under the new names.

  Two collisions forced it. Serial discovery had just been renamed to
  `00_discover_dc_switch_serials.yml`, which put it on the same number as the
  orchestrator `00_dc_deploy.yml` — two stages of completely different kinds
  sharing a number, one a prerequisite the student invokes by name and the
  other the thing that runs everything else. And the deleted `02_discover_serials.yml`
  had left a hole at 02, so the sequence skipped a number for a reason no
  reader of the directory could see.

  The new scheme does more than close the gap. **Stages 02 through 05 are
  exactly what `01_dc_deploy.yml` imports, in the order it imports them, and
  06 and 07 are the two playbooks deliberately left outside it.** Before this,
  `06_remove.yml` was numbered `05` and therefore sat in the middle of the
  orchestrated range despite being a destructive prune the orchestrator must
  never run, so the imported sequence read 01, 03, 04, 06 and the numbers told
  the reader nothing. Now the number answers the question on its own: anything
  from 02 to 05 happens on a plain `ansible-playbook playbooks/01_dc_deploy.yml`,
  and anything numbered 06 or above only happens when you ask for it by name.
  `00_discover_dc_switch_serials.yml` keeps its zero for the reason it always
  had it — it is the one stage that reaches a switch over SSH rather than the
  Nexus Dashboard API, and it runs once per pod before the pipeline does.

  Nothing about what the stages do has changed, only what they are called.
  Both READMEs and the student guide were updated in the same pass.

- **02_data_center — stage 00 now generates `topology_switches.nac.yaml`
  instead of printing serials for the student to type into it.** This reverses
  the decision recorded in the serial-discovery entry earlier in this note,
  which has been annotated to point here rather than left to contradict it.
  The case for the manual step was that transcribing a serial is where the
  student learns what Nexus as Code keys a switch on. It does not survive
  contact with the failure mode it creates: rule 301 in the validate role only
  checks that `serial_number` is non-empty, so a mistyped or transposed serial
  passes validation and then fails much later inside the create role, with a
  message that names neither the serial nor the file. Copying five sixteen-
  character strings out of a terminal table by eye is exactly the work to hand
  to the machine, and what the model is keyed on is now taught by the
  generated file's own header and by the README, rather than by an error the
  student has to make first.

  After it has read the serials, the stage copies the tracked
  `topology_switches.nac.yaml.example` verbatim over
  `topology_switches.nac.yaml` and substitutes, per switch, the serial it read
  and the role declared in `inventory/group_vars/all/dc_switches.yml`.
  Substituting into a copy rather than rendering a template is deliberate:
  every comment, vPC ID and port-channel member in the example survives
  exactly as written, so the tracked file stays the readable description of
  the fabric's shape instead of decaying into Jinja. The role comes from the
  switch table and not from the example because that table is the single
  source of truth for the switch list, and taking it from anywhere else would
  let the generated model drift away from it.

  The generated file is pure build output and the stage treats it as such:
  **every run overwrites it silently, with no backup, no confirmation flag and
  no refusal if it already exists.** Its leading comment block is swapped for a
  generated-file header saying so, naming the stage that wrote it and the two
  files to edit instead — the `.example` for the shape of the model,
  `dc_switches.yml` for the switch table.

  Three checks are what make generating the file defensible, since a generator
  is only worth having if its output can be trusted without inspection. The
  stage asserts that every declared switch returned a serial *before* it
  writes anything, so an `UNREACHABLE` switch fails the run with nothing on
  disk and a partial model never reaches stage 03. The substitution is
  anchored on each `- name: <switch>` line rather than on list position, so
  reordering either source is harmless. And because an anchored regex that
  matches nothing is not an error to `ansible.builtin.replace` — it leaves the
  file alone and reports no change — the stage slurps the file back afterwards
  and asserts that no `REPLACE_ME` survived, that every serial it read is
  present, and that each switch carries its own serial and its declared role.
  A switch name that disagrees between `dc_switches.yml` and the `.example` is
  caught there rather than shipped into stage 03.

  One implementation detail worth recording because it is not obvious: the
  replacement uses `\g<1>` and `\g<2>`, not `\1` and `\2`. Nexus serial numbers
  can begin with a digit, and `\1` immediately followed by a `9` parses as
  group 19.

  Stage 03 keeps its own placeholder guard, because the file is on disk and
  nothing stops it being edited after the fact; its message now says the file
  is generated and to re-run stage 00, rather than telling you to transcribe.
  The generated file stays gitignored and the `.example` stays tracked, with
  the `.gitignore` comment updated to call it build output. Updated in the
  same pass: the stage 00 and `01_dc_deploy.yml` headers, the `dc_switches.yml`
  header, both READMEs and the student guide. All eight playbooks pass
  `ansible-playbook --syntax-check`.

  The collection README heading **"Why serial discovery is its own stage, and
  why you transcribe by hand"** becomes **"…, and why it generates the
  model"**, and the troubleshooting table's link to it moved with it. The old
  anchor is dead, so any bookmark to it needs updating; the new one is
  `#why-serial-discovery-is-its-own-stage-and-why-it-generates-the-model`.

- **02_data_center — the `nd` connection now allows 1000s, so the switch
  import stops dying at 30 seconds.** `inventory/group_vars/nd/connection.yml`
  gains `ansible_command_timeout: 1000` and `ansible_connect_timeout: 1000`.
  Without them the run failed every time at step `inventory` with
  `MODULE FAILURE` and, in the traceback,
  `ConnectionError: command timeout triggered, timeout value is 30 secs`.

  **It fails in stage 02, not stage 03.** `02_fabric.yml` is named for the
  fabric object but runs the same `cisco.nac_dc_vxlan.dtc.create` role as
  `03_create.yml` with no tag filter, so once stage 00 has generated
  `topology_switches.nac.yaml` — a prerequisite for either stage — the switch
  import happens in 02. Stage 03 only does the import itself when run alone,
  or when 02 was held to `--tags cr_manage_fabric`.

  The cause is the shape of the call rather than anything slow. The inventory
  step makes a blocking HTTP request — `dcnm_inventory.import_switches()`
  POSTs to
  `fabrics/{fabric}/inventory/discover?setAndUseDiscoveryCredForLan=true` —
  and Nexus Dashboard holds that request open while it SSHes to the switches
  in it, discovers them and imports them. There is one POST per switch
  **role**, because `group_diff_create_by_role()` collapses the switches into
  one payload per role with a comma-joined `seedIP` — three for this pod
  (leaves, spines, border). That is minutes of work behind each response, and
  the `ansible.netcommon.httpapi` persistent connection had no override, so it
  used its 30-second default `persistent_command_timeout` and tore the call
  down mid-discovery.

  Nothing in the error says so. `cisco.dcnm`'s httpapi connection plugin
  appends "Please verify your login credentials, access permissions and
  fabric details" to every exception the transport raises, so a timeout
  presents as a credential or fabric problem, and the credentials and the
  fabric were both fine — the failed run had already created the fabric
  object (`RETURN_CODE: 200`, `FABRIC-218000`) before it died at the next
  step.

  1000 is the vendor-required floor, not a chosen margin. `cisco.dcnm` ships
  a guard for exactly this failure: `plugins/action/dcnm_inventory.py`
  hard-fails with a clear message unless **both** `persistent_command_timeout`
  and `persistent_connect_timeout` are `>= 1000`, and the collection's own
  README sets these two keys to that value.

  The part worth writing down is why that guard was no help. `cisco.nac_dc_vxlan`
  0.9.0 does not invoke the dcnm modules the way a playbook would — it drives
  them from its consolidated `dtc.manage_resources` action plugin through
  `plugins/plugin_utils/ndfc_executor.py`, and only two modules are routed
  via their own action plugins:

  ```python
  MODULES_WITH_ACTION_PLUGINS = frozenset({'cisco.dcnm.dcnm_vrf', 'cisco.dcnm.dcnm_network'})
  ```

  `dcnm_inventory` is not in that set, so it goes straight to
  `_execute_module` and its action plugin — and therefore its timeout check —
  never runs. The student gets the bare 30-second `ConnectionError` with no
  hint that a timeout is configurable at all. So the two lines are
  load-bearing and nothing upstream will say so; `connection.yml` now carries
  the explanation in a comment for the next reader.

  Operationally: **the inventory step now sits with no output for several
  minutes, and that is the discovery call working, not a hang.** Do not
  interrupt it. A run that already failed this way is safe to repeat —
  `dcnm_inventory` runs `state: merged`, so a re-run converges whether or not
  Nexus Dashboard finished discovering server-side after Ansible walked away.
  On a checkout that predates this, one run can be fixed from the command
  line: `ansible-playbook playbooks/01_dc_deploy.yml -e ansible_command_timeout=1000 -e ansible_connect_timeout=1000`.

  Scope is the whole `nd` group, which `static_inventory.yml` gives both
  `Pseudoco-DC1` and `External`, deliberately: every controller-facing stage
  makes the same kind of call. The sibling
  `group_vars/dc_fabric_switches/connection.yml` already raised the **SSH**
  timeouts to 60s for stage 00; `group_vars/nd/` was the gap. No other
  collection in this repo uses `ansible.netcommon.httpapi`, so campus and
  assurance are unaffected. Both DC READMEs and the student guide gained the
  symptom, including the literal `command timeout triggered` text so it is
  findable by search.

  While correcting the stage attribution, the repo-root `README.md` was found
  describing the DC collection as "stages 01–06". It ships `00` through `07`,
  so the reading-order table now says `00–07`.

- **02_data_center — the six port-channel descriptions are ASCII now, because
  an em dash is the only thing we control in a request NDFC refused to
  render.** With the timeout above fixed, the switch import succeeded and the
  run then died at step 13 of 21, `interface_all` (`dcnm_interface`):
  `RETURN_CODE: 500` on `POST .../lan-fabric/rest/globalInterface`, with one
  `DATA` entry per interface reading "An unexpected error occurred during
  template execution. Please retry after some time." against
  `DC-Leaf2:DC-Leaf1:vPC3`, `vPC4` and `vPC5`. `"MAIN server — vPC3"` and its
  PROD/IOT siblings became `"MAIN server - vPC3"` in both
  `playbooks/host_vars/Pseudoco-DC1/topology_switches.nac.yaml.example` and
  `inventory/group_vars/all/dc_switches.yml`, which have to agree.

  The reasoning, rather than a guess at the message: the outgoing `nvPairs`
  were compared field by field against `cisco.nac_dc_vxlan` 0.9.0's own
  `roles/validate/files/defaults.yml`, and **every nvPair in the payload was
  the collection default** — `MTU: jumbo`, `SPEED: Auto`, `PC_MODE: active`,
  `LACP_PORT_PRIO: 32768`. That includes `PEER1_ACCESS_VLAN: '1'`, which
  looks like an anomaly beside this fabric's 2300–2999 network VLAN range and
  is not: it is upstream's `access_vlan: 1`. The only fields that were ours
  were the three descriptions, and the em dash in them was the **only
  non-ASCII byte in the entire HTTP request** — sitting in `PEER1_PO_DESC` /
  `PEER2_PO_DESC`, which NDFC feeds to a Velocity template to render CLI, in
  a call that failed with a *template execution* exception.

  **Recorded as a strong suspect, not a proven root cause, and the docs say
  so.** Cisco documents no character restriction on an interface description;
  the article for this controller's release, "Working with Connectivity in
  Your Nexus Dashboard LAN Fabrics" 4.2.1, raises only a 64-character
  truncation caveat on the Description field. The message is a generic
  catch-all that also appears in unrelated netascode issues #505 and #345 and
  in a Cisco Community thread on ND 4.1.1g about setting switch roles. The em
  dash is simply the one variable we control and it costs nothing to remove.
  The confound is worth stating too: those three port-channels are the only
  vPC interfaces in the model, so a run that now succeeds does not by itself
  distinguish an em-dash problem from anything else on the vPC path. If
  definitive evidence is wanted, NDFC logs this failure as an alarm under the
  `lan_fabric_errors` policy with the offending template named — Nexus
  Dashboard → Events/Alarms, filtered to the run time.

  **Operationally the important part is that `git pull` is not enough.** The
  descriptions that reach Nexus Dashboard come from the `.example`, because
  stage 00 copies it over `topology_switches.nac.yaml` and substitutes only
  `serial_number` and `role` — and that generated file is gitignored build
  output, so a pull leaves the old em dashes on the script server. Re-run
  `playbooks/00_discover_dc_switch_serials.yml` after pulling, with the
  **client VPN up**, since that is the one stage in the collection that SSHes
  to `198.18.128.x`. From
  `ansible-automation/02_data_center/nac_vxlan/ansible`: `git pull`, then
  stage 00, then `playbooks/01_dc_deploy.yml`. The same trap applies to any
  future edit of the `.example` or the switch table, so it is now a
  troubleshooting row in its own right. Both DC READMEs and the student guide
  gained the symptom, including the literal `globalInterface` and `template
  execution` strings so it is findable by search.

- **02_data_center - every VRF and network now pins its `vlan_id`, because
  letting Nexus Dashboard allocate them hands all three the same VLAN.** With
  the interface descriptions fixed, the run reached step 17 of 21, `networks`
  (`dcnm_network`), and died there after 120 seconds. The controller answered
  `RETURN_CODE: 200` and put the failure in the per-attachment `DATA` map:
  `MainNetwork1` SUCCESS, `ProdNetwork1` and `IOTNetwork1` both
  `Entered network VLAN id 2300 is already in use`, then a successful
  `ROLLBACK_RESULT`.

  The cause was read out of the installed collection rather than guessed. In
  `cisco.dcnm` 3.13.0, `plugins/modules/dcnm_vrf.py` lines 4011-4043 loop over
  the objects to create and, for any object whose VLAN is 0, GET
  `.../resource-manager/vlan/{fabric}?vlanUsageType=TOP_DOWN_VRF_VLAN`. That
  endpoint returns the next *free* VLAN and does not reserve it, and nothing is
  committed between iterations, so every object in one run is handed the bottom
  of the range. The first attachment wins and the rest are rejected.
  `dcnm_network.py` does the same in its create loop - GET at line 5056, path
  constant at line 1083 - against `TOP_DOWN_NETWORK_VLAN`, which is why all three networks
  asked for 2300.

  `vrfs.nac.yaml` gets MAIN 2000, PROD 2001, IOT 2002 and `networks.nac.yaml`
  gets MainNetwork1 2300, ProdNetwork1 2301, IOTNetwork1 2302, taken in
  declaration order from the ranges `cisco.nac_dc_vxlan` defaults to in
  `roles/validate/files/defaults.yml` and which this repo does not override:
  `layer3_vlan_range` 2000-2299 and `layer2_vlan_range` 2300-2999. Both header
  comments said the opposite of what the files now do and were rewritten.

  The part worth carrying to the next stage of this work: omitting the VLAN is
  safe when a human clicks **Propose VLAN** in the Nexus Dashboard GUI, because
  that is one object at a time with a save in between. It is not safe in a
  batched API run. So the guide's click sequence and the data model's
  automation of it are not equivalent here, and anywhere the guide leans on the
  controller allocating a resource interactively, the model has to pin what the
  GUI would have proposed.

  Operationally this one is simpler than the em-dash fix above: these two files
  are read directly by the create role and are not regenerated by stage 00, so
  a plain `git pull` is the whole fix - no discovery re-run, no VPN. Verified
  live twice on the pod. The first re-run completed all 21 steps,
  `Pseudoco-DC1 : ok=40 changed=4 unreachable=0 failed=0 skipped=17`, with no
  occurrence of "already in use" in the log; the second reported
  `vrfs -> ok (changed=False)` and `networks -> ok (changed=False)`, which is
  what proves the attachments landed rather than failing silently again.

- **02_data_center - a green `vrfs` step is not evidence that the VRFs
  attached, and both DC READMEs now say so.** Step 15 `vrfs` (`dcnm_vrf`) hit
  the VLAN collision above two steps before `networks` did, at VLAN 2000, and
  reported `ok (changed=True)` without failing the play: the module does not
  inspect the per-attachment `DATA` map inside an HTTP 200 body, so two of the
  three attachments were rejected by the controller in silence and the run
  only stopped later, indirectly, when `dcnm_network` hit the same wall and did
  fail. This is an observation about how this version of the module reports,
  not a filed bug, but it is the reason the failure was two steps away from
  where it was diagnosed - so when something downstream of `vrfs` complains
  about VRFs, read the `DATA` map in the `vrfs` response rather than trusting
  its `ok`. Added to the collection README's troubleshooting table as a row of
  its own, keyed on that symptom.

- **02_data_center - `topology_vpc.nac.yaml` now flags `domain_id` as the same
  latent hazard, without changing behaviour.** The vPC `domain_id` is still
  left to Nexus Dashboard to allocate, which is the identical read-without-
  reserve pattern that broke the VRF and network VLANs. It cannot bite today
  because the model declares exactly one `vpc_peers` entry, so there is
  nothing for it to collide with, and pinning it would be an unforced change
  to a resource the guide also leaves to the controller. The header comment
  now says all of that, and says to pin it before a second pair is declared.

- **docs - three pre-existing errors corrected in the manual section 6
  walkthrough, found while reviewing the VLAN card.** The VRF step named the
  fabric `Pseudoco-DC-1`, which does not exist and which a student types by
  hand; it is `Pseudoco-DC1`, as the Networks step three paragraphs later
  already had it. The Networks intro said "two networks" where the section
  creates three. And all three Network ID bullets said "This is the first VNI
  from the L2VNI pool", which is true only of 30000; 30001 and 30002 are now
  the second and third. Also corrected in the new automation card: the failing
  call is the attachment POST to `.../networks/attachments`, so the card no
  longer claims the rollback leaves the fabric looking untouched - the network
  objects themselves are created before the attachment is refused.

- **02_data_center — `02_fabric.yml` is deleted and the DC pipeline renumbered,
  because it was a strictly-less-safe duplicate of the create stage.**
  `02_fabric.yml` and `03_create.yml` ran an identical role list —
  `cisco.nac_dc_vxlan.validate` then `cisco.nac_dc_vxlan.dtc.create` — against
  the same host with no tag filter. The only real difference was that 03
  carried three `pre_tasks` guarding the generated switch model and 02 carried
  none, so on any pod past stage 00 the stage advertised as a cheap first step
  silently performed the entire build, unguarded, including the greenfield
  switch import. The remaining playbooks each move down one: `03_create.yml` →
  `02_create.yml`, `04_deploy.yml` → `03_deploy.yml`, `05_verify_fabric.yml` →
  `04_verify_fabric.yml`, `06_remove.yml` → `05_remove.yml` and
  `07_external_fabric.yml` → `06_external_fabric.yml`, with the verify stage's
  report template moving with it, `templates/stage05-verification.md.j2` →
  `templates/stage04-verification.md.j2`, so a run now leaves
  `evidence/stage04-verification.md`. `00_discover_dc_switch_serials.yml` and
  `01_dc_deploy.yml` keep their numbers, `01_dc_deploy.yml` now imports exactly
  three stages, and every rename went through `git mv` so the history follows
  the file.

  This is a consequence of the serial-discovery rewrite recorded earlier in
  this note, and it settles the loose end that entry left. `02_fabric.yml`
  existed to break a chicken-and-egg: the old discovery stage called the
  fabric-scoped `.../fabrics/{fabric}/inventory/test-reachability`, so the
  fabric had to exist before a switch could be discovered, while the switches
  were what the rest of the model needed. `00_discover_dc_switch_serials.yml`
  now reads the serials over SSH with `cisco.nxos.nxos_facts` and never
  contacts Nexus Dashboard at all, so the circle is gone and the stage that
  existed only to break it has nothing left to do. The earlier entry kept it
  "as an explicit cheap first step"; that was the wrong call, because by then
  it was neither cheap nor a step.

  Nothing in the mapping to the student guide is lost. Section 4's "Create a
  VXLAN Fabric" is still available on its own, as
  `ansible-playbook playbooks/02_create.yml --tags cr_manage_fabric`.
  `cr_manage_fabric` is a real per-step tag bound to the `fabric` step in the
  collection's `resources/create_resources.yml` rather than an approximation of
  one, and because stage 02's pre-tasks are tagged `always` they still run
  under the tag filter — so that invocation is fabric-only *with* the guards
  `02_fabric.yml` never had.

  One honest behavioural change: on a fresh checkout you can no longer create
  an empty fabric before running stage 00, because stage 02 refuses to start
  without the generated switch model. That is acceptable rather than a
  regression — stage 00 was already a hard prerequisite for everything else in
  the collection, so all that is lost is a way to do one step out of order.

  A good deal of documentation was **deleted** rather than renumbered, and the
  READMEs are shorter for it. Both DC READMEs carried paragraphs, and the
  collection's troubleshooting table carried three rows hedged as
  "Stage 02 (or 03)", which existed purely to explain which of two identical
  stages a given failure would land in. With one create stage the question
  does not arise and the answer is dead prose. The same change fixes a
  statement that had quietly been wrong: the collection README's "**The import
  erases switches, and you cannot turn that off**" attributed the wipe to
  stage 03 when the wipe actually happened in stage 02, and it now names the
  single stage that does it. The repo-root README's DC row moves from
  "stages 00–07" to "stages 00–06".
