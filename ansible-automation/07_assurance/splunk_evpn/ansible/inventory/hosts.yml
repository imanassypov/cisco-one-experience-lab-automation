---
# Unlike collections 00-01, this one has a real SSH target: the collector runs on
# the Splunk host, so its binary, config and systemd unit must be managed there.
# The host key must stay 'splunk_server' - it is the lab_access.yml vault key.
all:
  children:
    splunk_servers:
      hosts:
        splunk_server:
          ansible_host: 198.18.5.109
