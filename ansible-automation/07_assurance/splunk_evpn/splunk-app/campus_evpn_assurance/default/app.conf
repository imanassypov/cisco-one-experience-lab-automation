[launcher]
author = Network Operations
description = BGP EVPN VXLAN Fabric Operational Health — Cisco Catalyst Campus
version = 1.5.0

[package]
id = campus_evpn_assurance
check_for_updates = false

[ui]
is_visible = true
label = Campus EVPN Assurance

# build belongs here, not in an [app] stanza - Splunk logs "Invalid key in
# stanza [app]" on every startup otherwise. Bump it whenever a dashboard
# changes; stage 06 asserts the installed build matches this one.
[install]
state = enabled
build = 107
