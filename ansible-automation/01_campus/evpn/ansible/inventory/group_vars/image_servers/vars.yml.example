---
# Non-secret image-server config — copy to vars.yml (gitignored):
#   cp vars.yml.example vars.yml
#
# URLs served must match settings.json swim.image_server_base_url (stage 6.0).

image_server_ip: "198.18.134.28"

image_local_paths:
  - "/path/to/cat9kv-universalk9.BLD_V262_THROTTLE_LATEST_20260529_003538.SSA.bin"
  - "/path/to/cat9kv-universalk9.17.15.03.SPA.bin"

# Optional overrides (defaults in roles/http_image_server/defaults/main.yml)
# image_web_root: /var/www/html
# image_url_subdir: images
# manage_ufw: true
