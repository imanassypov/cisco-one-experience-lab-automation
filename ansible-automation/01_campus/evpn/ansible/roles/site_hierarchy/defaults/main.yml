---
# cisco.catalystcenter.buildings / floors PUT (collection 2.10.2 +
# catalystcentersdk 3.1.3.0.1) raises UnboundLocalError: local variable 'func'.
# Existing sites are left as-is. CREATE still runs for missing paths.
# Re-enable: -e site_hierarchy_update_existing=true
site_hierarchy_update_existing: false
