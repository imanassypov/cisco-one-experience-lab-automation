---
# =============================================================================
# swim / distribute.yml  —  Stage 06.3: push image to devices per site/role
# =============================================================================
# Pre:  swim_details.distribute_images (image imported + tagged in prior step)
# Post: distribution tasks submitted to CatC SWIM workflow manager
# =============================================================================

- name: Load SWIM image details from settings.json
  ansible.builtin.import_tasks: load_swim_details.yml

- name: Distribute image by site and role
  cisco.catalystcenter.swim_workflow_manager:
    catalystcenter_host: "{{ catalystcenter_host }}"
    catalystcenter_username: "{{ catalystcenter_username }}"
    catalystcenter_password: "{{ catalystcenter_password }}"
    catalystcenter_verify: "{{ catalystcenter_verify }}"
    catalystcenter_port: "{{ catalystcenter_port }}"
    catalystcenter_version: "{{ catalystcenter_version }}"
    catalystcenter_log: "{{ catalystcenter_log }}"
    catalystcenter_log_level: "{{ catalystcenter_log_level }}"
    catalystcenter_log_file_path: "{{ catalystcenter_log_file_path }}"
    catalystcenter_log_append: "{{ catalystcenter_log_append }}"
    validate_response_schema: "{{ validate_response_schema }}"
    catalystcenter_api_task_timeout: "{{ catalystcenter_api_task_timeout }}"
    catalystcenter_task_poll_interval: "{{ catalystcenter_task_poll_interval }}"
    config_verify: true
    state: merged
    config:
      - image_distribution_details: "{{ item.image_distribution_details }}"
  loop: "{{ swim_details.distribute_images }}"
  loop_control:
    label: "{{ item.name }}"
  register: distribute_result

- name: Write distribution evidence
  ansible.builtin.include_tasks: write_evidence.yml
  vars:
    evidence_name: 20_distribute
    evidence_data: "{{ distribute_result }}"
