---
# =============================================================================
# swim / main.yml
# =============================================================================
# Stub entry — playbooks 06.x_swim_*.yml invoke action task files directly via
# import_role tasks_from: preflight | import_and_tag | distribute | activate |
#                         rollback | postcheck
# =============================================================================

- name: Dispatch SWIM action
  ansible.builtin.include_tasks: "{{ swim_action }}.yml"
