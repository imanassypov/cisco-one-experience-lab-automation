{## CATC: productFamily=Switches and Hubs, softwareType=IOS-XE, productSeries=Cisco Catalyst 9000 Series Virtual Switches ##}
{# FABRIC-VRF.j2 - Standalone VRF Definition Template #}
{# Generates VRF definitions for the current device #}
{% include "{{ TEMPLATE_PROJECT_NAME }}/DEFN-VRF.j2" %}
{% include "{{ TEMPLATE_PROJECT_NAME }}/FUNC-VRF-LOOKUP.j2" %}
{% include "{{ TEMPLATE_PROJECT_NAME }}/DEFN-ROLES.j2" %}
{% include "{{ TEMPLATE_PROJECT_NAME }}/DEFN-LOOPBACKS.j2" %}
{% include "{{ TEMPLATE_PROJECT_NAME }}/DEFN-BORDER-DMZ-TUNNELS.j2" %}
{% include "{{ TEMPLATE_PROJECT_NAME }}/DEFN-OVERLAY.j2" %}
{# Dereference Catalyst Center magic variable #}
{% set DEVICE_HOSTNAME = __device.hostname | default('', true) %}
{# Resolve the VRF objects for this device #}
{% set vrf_objs = [] %}
{{ vrf_definition(DEFN_VRF, DEFN_VRF_TO_NODE, DEVICE_HOSTNAME) }}
{# Dummy set to satisfy CatC static UDV scanner - overridden at runtime by for-loop #}
{% set vrf = {'name': '', 'id': '', 'mdt_default': '', 'mdt_data': ''} %}
{% for vrf in vrf_objs %}
vrf definition {{ vrf.name }}
description VRF {{ vrf.name }} definition
rd {{ DEFN_LOOP_UNDERLAY[DEVICE_HOSTNAME] }}:{{ vrf.id }}
!
address-family ipv4
{% if vrf.mdt_default %}
mdt auto-discovery vxlan
mdt default vxlan {{ vrf.mdt_default }}
mdt data vxlan {{ vrf.mdt_data }} 0.0.0.255
mdt data threshold 1
mdt overlay use-bgp
{% endif %}
route-target export {{ FABRIC_BGP_ASN }}:{{ vrf.id }}
route-target import {{ FABRIC_BGP_ASN }}:{{ vrf.id }}
route-target export {{ FABRIC_BGP_ASN }}:{{ vrf.id }} stitching
route-target import {{ FABRIC_BGP_ASN }}:{{ vrf.id }} stitching
{# Border-only: import rewritten DMZ shared RT into tenant VRFs #}
{% if DEVICE_HOSTNAME in DEFN_NODE_ROLES['BORDER'] and DEFN_TUNNELS is defined and DEFN_TUNNELS | length > 0 %}
{% for extra_rt in DEFN_VRF_BORDER_IMPORT_RTS %}
route-target import {{ FABRIC_BGP_ASN }}:{{ extra_rt }}
route-target import {{ FABRIC_BGP_ASN }}:{{ extra_rt }} stitching
{% endfor %}
{% endif %}
exit-address-family
!
{% endfor %}