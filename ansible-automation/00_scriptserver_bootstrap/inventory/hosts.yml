---
# Collection 00 runs ON the script server, so this host is the box itself.
# The name must stay 'script_server' - it is the lab_access.yml vault key.
all:
  children:
    script_servers:
      hosts:
        script_server:
          ansible_host: 127.0.0.1
