# Script server pins. Installed into ~/venv by
# 00_scriptserver_bootstrap/stage-script-server.sh, then topped up by the
# bootstrap role (which adds genie and pyats).
ansible-core==2.17.14
paramiko==3.5.1
netaddr==1.3.0
# CatC 3.1.5 appliance; SDKs expose API profile 3.1.3.0 only (no 3.1.5).
catalystcentersdk==3.1.3.0.1
dnacentersdk==2.10.6
# Renders the stage 10 markdown report in the terminal:
#   python -m rich.markdown evidence/stage10-verification.md | less -R
# Pure Python, so it installs from pip on an image whose apt cannot supply a
# renderer like glow without upgrading libc6.
rich==15.0.0
